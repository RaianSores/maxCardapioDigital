import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        padding: 10,
        borderRadius: 8,
        marginBottom: 20,
        alignSelf: 'center',
    },
    message: {
        color: 'white',
        fontSize: 28,
    },
});